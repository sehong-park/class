<%= form_for(@example_answer) do |f| %>
  <% if @example_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@example_answer.errors.count, "error") %> prohibited this example_answer from being saved:</h2>

      <ul>
      <% @example_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :example_answer %><br>
    <%= f.text_field :example_answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
