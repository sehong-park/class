<%= form_for(@material) do |f| %>

<div class="form-group <%= bs_validation(:name, @material) %>">
  <%= f.label(:name, t('activerecord.attributes.material.name')) %>
  <%= f.text_field(:name, class: "form-control input-lg", placeholder: t('form.placeholder.name', model: t('activerecord.models.material'))) %>
  <%= bs_validation_icon(:name, @material) %>
  <%= bs_validation_message(:name, @material.errors) %>
</div>

<div class="form-group <%= bs_validation(:description, @material) %>">
  <%= f.label(:description, t('activerecord.attributes.material.description')) %>
  <%= f.text_area(:description, rows: 3, class: "form-control input-lg", placeholder: t('form.placeholder.description', model: t('activerecord.models.material'))) %>
  <%= bs_validation_icon(:description, @material) %>
  <%= bs_validation_message(:description, @material.errors) %>
</div>

<div class="form-group <%= bs_validation(:link, @material) %>">
  <%= f.label(:link, t('activerecord.attributes.material.link')) %>
  <%= f.text_field(:link, class: "form-control input-lg", placeholder: t('form.placeholder.link', model: t('activerecord.models.material'))) %>
  <%= bs_validation_icon(:link, @material) %>
  <%= bs_validation_message(:link, @material.errors) %>
</div>

<div class="form-group <%= bs_validation(:image, @material) %>">
  <%= f.label(:image, t('activerecord.attributes.material.image')) %>
  <%= f.file_field :image %>
  <% unless @material.image.url.include?(Lecture::DEFAULT_IMAGE) %>
  <%= image_tag(@material.image.url, alt: @material.name) %>
  <% end %>
  <%= bs_validation_icon(:image, @material) %>
  <%= bs_validation_message(:image, @material.errors) %>
</div>

<div class="actions">
  <%= f.submit t('form.submit'), class: "btn btn-lg btn-form-control btn-primary" %>
</div>
<% end %>
